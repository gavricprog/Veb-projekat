@model List<string>

<h2>Terapije</h2>

@if (Model.Count > 0)
{
    <ul>
        @foreach (var therapy in Model)
        {
            <li>@therapy</li>
        }
    </ul>
}
else
{
    <p>Nema dostupnih terapija za ovog pacijenta.</p>
}
